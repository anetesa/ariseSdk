# Создание GitHub Release для AriseMobileSdk-v2

## Пошаговая инструкция

### Шаг 1: Подготовка релиза

#### 1.1. Убедитесь, что все готово

```bash
cd Distribution/AriseMobileSdk-v2

# Проверьте, что все бинарники на месте
ls -la libs/
# Должны быть:
# - AriseMobileSdk.xcframework
# - CloudCommerce.xcframework

# Проверьте Package.swift
cat Package.swift

# Убедитесь, что все изменения закоммичены
cd ../..
git status
```

#### 1.2. Обновите версию (если нужно)

Если это новый релиз, обновите версию:

```bash
# Используйте скрипт создания версии
cd Distribution/AriseMobileSdk-v2
./create_version.sh 2.0.1

# Или вручную:
# 1. Обновите версию в Info.plist (если используется)
# 2. Соберите новый XCFramework
# 3. Скопируйте бинарники
```

#### 1.3. Закоммитьте изменения

```bash
cd ../..
git add .
git commit -m "Release version 2.0.1"
git push origin main
```

### Шаг 2: Создание Git тега

#### 2.1. Создайте аннотированный тег

```bash
# Аннотированный тег (рекомендуется)
git tag -a v2.0.1 -m "Release AriseMobileSdk v2.0.1

Features:
- Added new transaction methods
- Improved error handling

Bug fixes:
- Fixed authentication issue
- Resolved memory leak"

# Или простой тег
git tag v2.0.1
```

#### 2.2. Отправьте тег в GitHub

```bash
# Отправить один тег
git push origin v2.0.1

# Или отправить все теги
git push origin --tags
```

#### 2.3. Проверьте тег

```bash
# Посмотреть информацию о теге
git show v2.0.1

# Посмотреть все теги
git tag -l "v2.*"
```

### Шаг 3: Создание Release через GitHub Web Interface

#### 3.1. Перейдите на страницу Releases

1. Откройте ваш репозиторий на GitHub
2. Нажмите на **Releases** (справа в меню, или URL: `https://github.com/your-org/arise-merchant-app/releases`)
3. Нажмите **Draft a new release** или **Create a new release**

#### 3.2. Заполните форму Release

**Tag version:**
- Выберите существующий тег `v2.0.1` из выпадающего списка
- Или создайте новый тег прямо здесь (выберите "Create new tag: v2.0.1")

**Release title:**
```
AriseMobileSdk v2.0.1
```
или более описательно:
```
AriseMobileSdk iOS SDK v2.0.1 - Enhanced Transaction Support
```

**Description:**
```markdown
## 🚀 AriseMobileSdk v2.0.1

### 📦 Что нового

#### ✨ Новые возможности
- Добавлены новые методы для работы с транзакциями
- Улучшена обработка ошибок с более детальными сообщениями
- Добавлена поддержка дополнительных типов платежей

#### 🐛 Исправления ошибок
- Исправлена проблема с аутентификацией в некоторых случаях
- Устранена утечка памяти при длительных сессиях
- Исправлена проблема с обновлением токенов

#### 🔧 Улучшения
- Оптимизирована производительность сетевых запросов
- Улучшена документация API

### 📋 Технические детали

- **Минимальная версия iOS:** 15.0
- **Минимальная версия Xcode:** 15.0
- **Минимальная версия Swift:** 5.9

### 📥 Установка

```swift
dependencies: [
    .package(
        url: "https://github.com/your-org/arise-merchant-app.git",
        from: "2.0.1"
    )
]
```

### 🔗 Ссылки

- [Документация](https://docs.your-org.com/arise-mobile-sdk)
- [Changelog](https://github.com/your-org/arise-merchant-app/blob/main/CHANGELOG.md)
- [Миграция с v1.x](https://docs.your-org.com/migration-guide)
```

#### 3.3. Прикрепите файлы (опционально)

В разделе **Attach binaries** можно прикрепить:
- `.zip` архив с XCFramework
- `.tar.gz` архив
- Release notes в формате PDF

**Пример:**
```
📎 Attach files:
- AriseMobileSdk-v2.0.1-xcframeworks.zip
- Release-Notes-v2.0.1.pdf
```

#### 3.4. Настройки Release

- **Set as the latest release:** ✅ (для главного релиза)
- **Set as a pre-release:** ❌ (снимите галочку для стабильного релиза)
  - Используйте pre-release для beta/alpha версий

#### 3.5. Опубликуйте Release

Нажмите **Publish release**

### Шаг 4: Создание Release через GitHub CLI (альтернатива)

Если предпочитаете командную строку:

#### 4.1. Установите GitHub CLI

```bash
# macOS
brew install gh

# Проверьте установку
gh --version
```

#### 4.2. Авторизуйтесь

```bash
gh auth login
# Следуйте инструкциям
```

#### 4.3. Создайте Release

```bash
# Простой релиз
gh release create v2.0.1 \
  --title "AriseMobileSdk v2.0.1" \
  --notes "Release notes here"

# С подробными заметками из файла
gh release create v2.0.1 \
  --title "AriseMobileSdk v2.0.1" \
  --notes-file RELEASE_NOTES.md

# С прикрепленными файлами
gh release create v2.0.1 \
  --title "AriseMobileSdk v2.0.1" \
  --notes-file RELEASE_NOTES.md \
  dist/AriseMobileSdk-v2.0.1.zip

# Pre-release
gh release create v2.0.1-beta \
  --title "AriseMobileSdk v2.0.1 Beta" \
  --notes "Beta release" \
  --prerelease
```

### Шаг 5: Шаблон Release Notes

Создайте файл `RELEASE_NOTES_TEMPLATE.md`:

```markdown
## 🚀 AriseMobileSdk v{{VERSION}}

### 📦 Что нового

#### ✨ Новые возможности
- 

#### 🐛 Исправления ошибок
- 

#### 🔧 Улучшения
- 

#### ⚠️ Breaking Changes (если есть)
- 

### 📋 Миграция

Если обновляетесь с предыдущей версии, проверьте [руководство по миграции](link).

### 📥 Установка

\`\`\`swift
dependencies: [
    .package(
        url: "https://github.com/your-org/arise-merchant-app.git",
        from: "{{VERSION}}"
    )
]
\`\`\`

### 🔗 Полезные ссылки

- [Документация](link)
- [Changelog](link)
- [Issues](link)

### 👥 Contributors

Спасибо всем, кто принял участие в этом релизе!

---

**Полный список изменений:** [CHANGELOG.md](link)
```

### Шаг 6: Автоматизация через GitHub Actions

Создайте `.github/workflows/release.yml`:

```yaml
name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Get version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Tag: ${{ github.ref }}"
          echo "Version: $VERSION"

      - name: Generate release notes
        id: release_notes
        run: |
          # Автоматически генерируйте release notes из коммитов
          NOTES=$(git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^)..HEAD)
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ steps.get_version.outputs.version }}
          body: |
            ## AriseMobileSdk v${{ steps.get_version.outputs.version }}
            
            ### Changes:
            ${{ steps.release_notes.outputs.notes }}
          draft: false
          prerelease: false
```

### Шаг 7: Лучшие практики

#### 7.1. Семантическое версионирование

```
MAJOR.MINOR.PATCH
```

- **MAJOR** (3.0.0): Breaking changes
- **MINOR** (2.1.0): Новые функции, обратно совместимые
- **PATCH** (2.0.1): Исправления ошибок

#### 7.2. Формат тегов

```
v2.0.1  ✅ Правильно
2.0.1   ❌ Неправильно (без префикса v)
v2.0    ⚠️  Возможно, но лучше избегать
```

#### 7.3. Release Notes должны содержать

- ✅ Краткое описание изменений
- ✅ Список новых возможностей
- ✅ Список исправлений
- ✅ Инструкции по миграции (если есть breaking changes)
- ✅ Информацию о требованиях
- ✅ Примеры использования

#### 7.4. Регулярность релизов

- **Patch** релизы: по мере необходимости (критические баги)
- **Minor** релизы: раз в 1-2 месяца
- **Major** релизы: при необходимости, с миграцией

### Шаг 8: Проверка после создания Release

1. ✅ Проверьте, что Release виден на странице Releases
2. ✅ Проверьте, что тег создан правильно
3. ✅ Проверьте, что SPM может найти пакет:
   ```bash
   swift package resolve
   ```
4. ✅ Проверьте в Xcode:
   - File → Add Package Dependencies
   - Введите URL репозитория
   - Убедитесь, что версия доступна

### Шаг 9: Уведомления команде

После создания Release:

1. Уведомите команду через:
   - Slack/Teams канал
   - Email
   - Внутреннюю wiki

2. Обновите документацию:
   - README.md (если нужно)
   - CHANGELOG.md
   - Внутреннюю документацию

### Пример полного процесса

```bash
# 1. Подготовка
cd Distribution/AriseMobileSdk-v2
./create_version.sh 2.0.1

# 2. Коммит
cd ../..
git add .
git commit -m "Release version 2.0.1"
git push origin main

# 3. Создание тега
git tag -a v2.0.1 -m "Release AriseMobileSdk v2.0.1"
git push origin v2.0.1

# 4. Создание Release через CLI (или через веб-интерфейс)
gh release create v2.0.1 \
  --title "AriseMobileSdk v2.0.1" \
  --notes-file RELEASE_NOTES.md
```

### Полезные команды

```bash
# Посмотреть все релизы
gh release list

# Посмотреть детали релиза
gh release view v2.0.1

# Удалить релиз (осторожно!)
gh release delete v2.0.1

# Скачать релиз
gh release download v2.0.1

# Редактировать релиз
gh release edit v2.0.1 --notes "Updated notes"
```










